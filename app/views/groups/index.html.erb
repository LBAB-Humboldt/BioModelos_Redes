<h1>Indice de Grupos</h1>
<table>

  <% @groups.each do |group| %>
      <tr>
        <td><%= link_to image_tag(group.logo.url(:thumb250).to_s), group %></td>
        <td><%= link_to group.name, group %></td>
        <td><%= group.description %></td>
      </tr>
  <% end %>
</table>

<br>
